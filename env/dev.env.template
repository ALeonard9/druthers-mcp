# Copy to env/dev.env (gitignored) and fill in.
LZ=m3
ENV=dev
LOG_LEVEL=INFO

# aleonard.us-api base URL.
API_BASE_URL=http://127.0.0.1:8000

# Authenticate with EITHER a pre-issued token...
# API_TOKEN=

# ...or email + password (exchanged for a token at startup).
API_EMAIL=you@example.com
API_PASSWORD=change-me
