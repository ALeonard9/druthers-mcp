#!/bin/sh
# Launcher for the Druthers MCP server (stdio). Sources local env + venv.
cd "$(dirname "$0")/.." || exit 1
if [ -f env/local.env ]; then set -a; . ./env/local.env; set +a; fi
exec .venv/bin/python -m druthers_mcp.server
